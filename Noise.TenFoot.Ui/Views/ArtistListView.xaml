<UserControl x:Class="Noise.TenFoot.Ui.Views.ArtistListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:controls="clr-namespace:Noise.TenFoot.Ui.Controls.LoopingListBox"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Dto="clr-namespace:Noise.Infrastructure.Dto;assembly=Noise.Infrastructure"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ViewModels="clr-namespace:Noise.TenFoot.Ui.ViewModels"
             cal:Bind.AtDesignTime="True"
             d:DataContext="{d:DesignInstance ViewModels:ArtistListViewModel,
                                              IsDesignTimeCreatable=True}"
             d:DesignHeight="500"
             d:DesignWidth="700"
             mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="BasicArtist" DataType="{x:Type Dto:DbArtist}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           FontSize="18"
                           Text="{Binding Path=Name}" />
                <TextBlock Grid.Row="1"
                           Margin="0,0,7,0"
                           Text="{Binding Path=AlbumCount}"
                           TextAlignment="Right" />
            </Grid>
        </DataTemplate>

        <Style x:Key="ArtistItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Margin" Value="3,3" />
            <Setter Property="Width" Value="200" />
            <Setter Property="Height" Value="200" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Grid Background="LightBlue">
                            <ContentPresenter x:Name="ContentHost"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Artist List View" />

        <controls:LoopingWrapBox x:Name="ArtistList"
                                 Grid.Row="1"
                                 HorizontalContentAlignment="Stretch"
                                 VerticalContentAlignment="Stretch"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ItemContainerStyle="{StaticResource ArtistItemStyle}"
                                 ItemTemplate="{StaticResource BasicArtist}"
                                 LastItemMargin="1.25"
                                 Orientation="Horizontal"
                                 RelativeOffset="0.5"
                                 Offset="0.0" />

        <Button x:Name="Done"
                Grid.Row="2"
                Width="120"
                HorizontalAlignment="Left"
                Content="Return" />

        <Button x:Name="Home"
                Grid.Row="3"
                Width="120"
                HorizontalAlignment="Left"
                Content="Home" />
    </Grid>
</UserControl>
