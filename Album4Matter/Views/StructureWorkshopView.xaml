<UserControl x:Class="Album4Matter.Views.StructureWorkshopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Mvvm="http://prismlibrary.com/"
             Mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Style="{StaticResource ControlTitleStyle}" Text="Input"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button Margin="3,5" Style="{StaticResource GeneralButtonStyle}" Content="Add Files..." Command="{Binding BrowseFiles}"/>
                <Button Margin="3,5" Style="{StaticResource GeneralButtonStyle}" Content="Add Folders..." Command="{Binding BrowseFolders}"/>
            </StackPanel>

            <ListBox Grid.Row="2"/>
        </Grid>
    </Grid>
</UserControl>
