<UserControl x:Class="Noise.UI.Views.PlayingArtistView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 mc:Ignorable="d" 
			 d:DesignHeight="100" d:DesignWidth="300">
	
	<UserControl.DataContext>
		<Binding Path="PlayingArtistViewModel" Source="{StaticResource ViewModelLocator}" />
	</UserControl.DataContext>

	<Grid>
		<Viewbox Margin="9,0" Stretch="Uniform" StretchDirection="DownOnly" HorizontalAlignment="Right">
			<TextBlock Style="{StaticResource LargeTitleStyle}" FontSize="800" HorizontalAlignment="Right" Text="{Binding CurrentArtist.Name}" />
		</Viewbox>
	</Grid>
</UserControl>
