<UserControl x:Class="Noise.UI.Views.TrackStrategyOptionsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Noise.UI.Views"
             mc:Ignorable="d" 
             Width="350"
             Height="80"
             Tag="Strategy Play Options"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" Text="Play adjacent tracks:"/>
        <CheckBox Grid.Row="2" Margin="8,3" IsChecked="{Binding PlayPrevious}" Content="Play the previous track first"/>
        <CheckBox Grid.Row="3" Margin="8,3" IsChecked="{Binding PlayNext}" Content="Continue with the next track"/>
    </Grid>
</UserControl>
