<UserControl x:Class="Noise.UI.Views.ArtistTracksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Dto="clr-namespace:Noise.UI.Dto" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.DataContext>
        <Binding Path="ArtistTracksViewModel" Source="{StaticResource ViewModelLocator}"/>
    </UserControl.DataContext>

    <GroupBox BorderThickness="1" BorderBrush="{DynamicResource TrimColor}" Margin="0">
        <GroupBox.Header>
            <StackPanel Orientation="Horizontal">
                <Button Content="{StaticResource Path_SwitchView}" Height="19" Width="21" Margin="0,0,0,2" Command="{Binding Path=SwitchView}"/>
                <TextBlock Margin="5,0" Text="Tracks"/>
            </StackPanel>
        </GroupBox.Header>

        <Grid>
            <Grid Grid.IsSharedSizeScope="True">
                <Grid.RowDefinitions>
                    <!-- Header row -->
                    <RowDefinition Height="Auto" />
                    <!-- Row for data -->
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid.Resources>
                    <!-- Template for information at all levels -->
                    <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}" DataType="{x:Type Dto:UiArtistTrackNode}">
                        <Grid>
                            <!-- All column widths are shared except for column 1 which is sized to compensate for different indentation at each level -->
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition SharedSizeGroup="rowHeaderColumn"/>
                                <ColumnDefinition/>
                                <ColumnDefinition SharedSizeGroup="column1"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="{Binding Path=Track.Name}"/>
                            <Rectangle Grid.Column="1">
                                <Rectangle.Width>
                                    <MultiBinding Converter="{StaticResource LevelConverter}">
                                        <Binding Path="Level"/>
                                        <Binding ElementName="treeViewItemToMeasure" Path="ActualWidth"/>
                                    </MultiBinding>
                                </Rectangle.Width>
                            </Rectangle>
                            <TextBlock Grid.Column="2" Text="{Binding Path=AlbumName}"/>
                            <TextBlock Grid.Column="3" Text=""/>
                        </Grid>
                    </HierarchicalDataTemplate>
                </Grid.Resources>

                <!-- Tree view with one item for the header row -->
                <TreeView BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <TreeViewItem>
                        <TreeViewItem.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition SharedSizeGroup="rowHeaderColumn"/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition SharedSizeGroup="column1"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Track Name"/>
                                <TreeViewItem Grid.Column="1">
                                    <TreeViewItem.Header>
                                        <TreeViewItem Name="treeViewItemToMeasure" Padding="0"/>
                                    </TreeViewItem.Header>

                                    <!-- Set the width of Column 1 to the same width as the top level in the data -->
                                    <TreeViewItem.Width>
                                        <MultiBinding Converter="{StaticResource LevelConverter}">
                                            <Binding Path="Level"/>
                                            <Binding ElementName="treeViewItemToMeasure" Path="ActualWidth"/>
                                        </MultiBinding>
                                    </TreeViewItem.Width>
                                </TreeViewItem>
                                <TextBlock Grid.Column="2" Text="Album Name"/>
                            </Grid>
                        </TreeViewItem.Header>
                    </TreeViewItem>
                </TreeView>

                <!-- Tree view that will display hierarchical data rows -->
                <TreeView Grid.Row="1" BorderThickness="0" ItemsSource="{Binding Path=TrackList}"/>
            </Grid>
        </Grid>
    </GroupBox>
</UserControl>
