<UserControl x:Class="Noise.UI.Views.ArtistTracksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Behaviours="clr-namespace:Noise.UI.Behaviours" mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.DataContext>
        <Binding Path="ArtistTracksViewModel" Source="{StaticResource ViewModelLocator}"/>
    </UserControl.DataContext>

    <GroupBox BorderThickness="1" BorderBrush="{DynamicResource TrimColor}" Margin="0">
        <GroupBox.Header>
            <StackPanel Orientation="Horizontal">
                <Button Content="{StaticResource Path_SwitchView}" Height="19" Width="21" Margin="0,0,0,2" Command="{Binding Path=SwitchView}"/>
                <TextBlock Margin="5,0" Text="Tracks"/>
            </StackPanel>
        </GroupBox.Header>

        <ListView ItemsSource="{Binding Path=TrackList}" BorderThickness="0"
                          Behaviours:ListViewColumns.Stretch="true"
                          Behaviours:GridViewSort.AutoSort="True">
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn HeaderTemplate="{StaticResource PlayColumnHeaderTemplate}" Width="27">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding Path=Track.Play}" Style="{StaticResource PlayButtonStyle}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Track Name" DisplayMemberBinding="{Binding Path=Track.Name}"/>
                    <GridViewColumn Header="Album Name" DisplayMemberBinding="{Binding Path=AlbumName}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </GroupBox>
</UserControl>
