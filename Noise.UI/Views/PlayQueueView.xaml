<UserControl x:Class="Noise.UI.Views.PlayQueueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ValueConverters="clr-namespace:Noise.UI.ValueConverters" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Control.Resources>
        <ValueConverters:TimeSpanConverter x:Key="TimeSpanConverter"/>
    </Control.Resources>

    <ListView x:Name="PlayQueueList" ItemsSource="{Binding Path=QueueList}">
        <ListView.ItemContainerStyle>
            <Style TargetType="{x:Type ListViewItem}">
                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
            </Style>
        </ListView.ItemContainerStyle>
        <ListView.View>
            <GridView>
                <GridViewColumn Header="|>" DisplayMemberBinding="{Binding Path=IsPlaying}" />
                <GridViewColumn Header="Track Name" DisplayMemberBinding="{Binding Path=Track.Name}" />
                <GridViewColumn Header="Duration" DisplayMemberBinding="{Binding Path=Track.Duration, Converter={StaticResource TimeSpanConverter}}" />
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
