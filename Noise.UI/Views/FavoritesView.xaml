<UserControl x:Class="Noise.UI.Views.FavoritesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Behaviours="clr-namespace:Noise.UI.Behaviours" mc:Ignorable="d"
             Tag="Favorites"
             DataContext="{Binding FavoritesViewModel, Source={StaticResource ViewModelLocator}}"
             d:DesignHeight="300" d:DesignWidth="300">

    <GroupBox BorderThickness="1" BorderBrush="{DynamicResource TrimColor}" Header=" Favorites "  Margin="0">
        <ListView Grid.Row="0" BorderThickness="0" ItemsSource="{Binding Path=FavoritesList}"
            Behaviours:ListViewColumns.Stretch="true"
            Behaviours:GridViewSort.AutoSort="True">
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="|>" Width="27">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="12" Height="12" Command="{Binding Path=PlayFavorite}"
                                        BorderThickness="0" FontFamily="Webdings" Content="4" FontSize="8" HorizontalAlignment="Center"
                                        Style="{DynamicResource PlayButtonStyle}" Foreground="Gray"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Path=Title}" Behaviours:GridViewSort.PropertyName="Title"/>
                </GridView>
            </ListView.View>
        </ListView>
    </GroupBox>
</UserControl>
