<UserControl x:Class="Noise.UI.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ValueConverters="clr-namespace:Noise.UI.ValueConverters" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding PlayerViewModel, Source={StaticResource ViewModelLocator}}"             
             >
    <Control.Resources>
        <ValueConverters:TimeSpanConverter x:Key="TimeSpanConverter"/>
        <ValueConverters:VolumeLevelConverter x:Key="VolumeLevelConverter"/>
    </Control.Resources>

    <Grid x:Name="PlayerViewRoot">
        <Grid x:Name="Layout">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                <TextBlock Text="Current Track: " />
                <TextBlock Text="{Binding Path=TrackName}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Height="35">
                <Button Margin="3" Command="{Binding Path=PlayCommand}">Play</Button>
                <Button Margin="3" Command="{Binding Path=PauseCommand}">Pause</Button>
                <Button Margin="3" Command="{Binding Path=StopCommand}">Stop</Button>
                <Button Margin="3" Command="{Binding Path=PreviousTrackCommand}">Prev Track</Button>
                <Button Margin="3" Command="{Binding Path=NextTrackCommand}">Next Track</Button>
                <Button Margin="3" Command="{Binding Path=ClearQueueCommand}">Clear Queue</Button>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Position: "/>
                    <TextBlock Text="{Binding Path=TrackPosition, Converter={StaticResource TimeSpanConverter}}"/>
                </StackPanel>

                <Grid Margin="10,5,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="11"/>
                        <RowDefinition Height="11"/>
                    </Grid.RowDefinitions>

                    <Rectangle Grid.Row="0" HorizontalAlignment="Left" Fill="LightGreen" Margin="3" Height="7" Width="{Binding Path=LeftLevel, Converter={StaticResource VolumeLevelConverter}}"/>
                    <Rectangle Grid.Row="1" HorizontalAlignment="Left" Fill="LightGreen" Margin="3" Height="7" Width="{Binding Path=RightLevel, Converter={StaticResource VolumeLevelConverter}}"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
