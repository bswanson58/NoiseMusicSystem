<UserControl x:Class="Noise.UI.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ValueConverters="clr-namespace:Noise.UI.ValueConverters" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Control.Resources>
        <ValueConverters:TimeSpanConverter x:Key="TimeSpanConverter"/>
    </Control.Resources>
    
    <Grid x:Name="PlayerViewRoot">
        <Grid x:Name="Layout">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                <TextBlock Text="Current Track: " />
                <TextBlock Text="{Binding Path=TrackName}"/>
            </StackPanel>
            
            <StackPanel Grid.Row="1" Orientation="Horizontal" Height="27">
                <Button Command="{Binding Path=PlayCommand}">Play</Button>
                <Button Command="{Binding Path=PauseCommand}">Pause</Button>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Position: "/>
                    <TextBlock Text="{Binding Path=TrackPosition, Converter={StaticResource TimeSpanConverter}}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
