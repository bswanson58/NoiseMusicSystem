<UserControl x:Class="Noise.UI.Views.SearchViewDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
            Height="600" Width="800"
            Tag="Search"
             >
    <Grid Margin="3">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Margin="3" Text="Search Text:"/>
            <TextBox Grid.Column="1" Margin="3" Text="{Binding Path=SearchText, Mode=TwoWay}"/>
            <Button Grid.Column="2" Margin="3" Command="{Binding Path=Search}">Search</Button>
        </Grid>
        
        <ListView Grid.Row="1" ItemsSource="{Binding Path=SearchResults}">
            
        </ListView>
    </Grid>
</UserControl>
