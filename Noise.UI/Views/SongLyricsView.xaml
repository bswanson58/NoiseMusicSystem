<UserControl x:Class="Noise.UI.Views.SongLyricsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:behaviours="clr-namespace:Noise.UI.Behaviours"
             xmlns:controls="clr-namespace:Noise.UI.Controls"
             xmlns:eventCommandTriggers="clr-namespace:Noise.UI.Behaviours.EventCommandTriggers"
             xmlns:reusableBehaviours="clr-namespace:ReusableBits.Ui.Behaviours;assembly=ReusableBits.Ui"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewModels="clr-namespace:Noise.UI.ViewModels"
             mc:Ignorable="d" 
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance viewModels:SongLyricsViewModel, IsDesignTimeCreatable=False}"
             reusableBehaviours:VisualStateAdapter.VisualState="{Binding VisualStateName}"
             d:DesignHeight="300" d:DesignWidth="450">
    
    <Grid x:Name="LayoutRoot">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">
                <VisualState x:Name="Closed">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="LayoutRoot">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Hidden}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
                <VisualState x:Name="Normal">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="LayoutRoot">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Border BorderThickness="1" CornerRadius="3" Margin="0">
            <Grid behaviours:GridUtils.ColumnDefinitions="*,Auto" Margin="5"
                  behaviours:GridUtils.RowDefinitions="Auto,*,Auto">
                
                <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal" Margin="7,0,0,0">
                    <TextBlock Text="Song lyrics for '"/>
                    <TextBlock Text="{Binding Path=SongName}"/>
                    <TextBlock Text="'"/>
                </StackPanel>

                <Button Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" Width="17" Height="17" Margin="0,1,1,5"
                        Content="{StaticResource Path_BalloonClose}" Command="{Binding Close}" />

                <Border Grid.Row="1" Grid.Column="0" BorderThickness="1" CornerRadius="5" Grid.ColumnSpan="2">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding Path=SongLyrics}" Margin="3"/>
                    </ScrollViewer>
                </Border>
                <Grid Grid.Row="2" Grid.Column="0"  Margin="0,5,0,0" Grid.ColumnSpan="2"
                      behaviours:GridUtils.ColumnDefinitions="5,Auto,*,9,Auto,Auto,Auto">
                    <TextBlock Grid.Column="1" Text="Other versions:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox Grid.Column="2" ItemsSource="{Binding PossibleLyrics}" SelectedItem="{Binding SelectedLyric}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Path=ArtistName}"/>
                                    <TextBlock Text="/"/>
                                    <TextBlock Text="{Binding Path=SongName}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button Grid.Column="4" Content="Select This Version" Padding="5,1" Command="{Binding SelectLyric}"/>
                    <controls:LinkLabel Grid.Column="5" HorizontalAlignment="Right" Padding="0" LinkLabelBehavior="HoverUnderline" Content="source" Margin="7,0,0,0" VerticalAlignment="Bottom">
                        <eventCommandTriggers:CommandSource.Trigger>
                            <eventCommandTriggers:EventCommandTrigger RoutedEvent="controls:LinkLabel.Click" Command="{Binding Path=LyricsSourceClicked}"/>
                        </eventCommandTriggers:CommandSource.Trigger>
                    </controls:LinkLabel>
                    <Button Grid.Column="6" Content="{StaticResource Path_Edit}" Command="{Binding Path=Edit}" Height="22" Margin="7,0,0,0"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
