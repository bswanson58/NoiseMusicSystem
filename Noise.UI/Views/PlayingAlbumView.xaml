<UserControl x:Class="Noise.UI.Views.PlayingAlbumView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:ValueConverters="clr-namespace:ReusableBits.Ui.ValueConverters;assembly=ReusableBits.Ui"
			 mc:Ignorable="d" 
			 d:DesignHeight="100" d:DesignWidth="300">
	
	<UserControl.DataContext>
		<Binding Path="PlayingAlbumViewModel" Source="{StaticResource ViewModelLocator}" />
	</UserControl.DataContext>

	<UserControl.Resources>
		<ValueConverters:ByteImageConverter x:Key="imageConverter" />
	</UserControl.Resources>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="3*"/>
			<ColumnDefinition Width="1*"/>
		</Grid.ColumnDefinitions>

		<Viewbox Grid.Column="0" Margin="9,0" Stretch="Uniform" StretchDirection="DownOnly" HorizontalAlignment="Left">
			<TextBlock Style="{StaticResource LargeTitleStyle}" FontSize="800" HorizontalAlignment="Left" Text="{Binding AlbumName}" />
		</Viewbox>

		<Image Grid.Column="1" MinWidth="50" MinHeight="50" Margin="7,7,3,0" HorizontalAlignment="Left" VerticalAlignment="Top" Stretch="Uniform"
			   Source="{Binding AlbumImage, Converter={StaticResource imageConverter}}"/>
	</Grid>
</UserControl>
