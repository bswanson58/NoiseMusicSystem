<UserControl x:Class="Noise.UI.Views.StreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Behaviours="clr-namespace:Noise.UI.Behaviours" xmlns:Controls="clr-namespace:Noise.UI.Controls" xmlns:EventCommandTriggers="clr-namespace:Noise.UI.Behaviours.EventCommandTriggers" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Tag="Radio"
             DataContext="{Binding StreamViewModel, Source={StaticResource ViewModelLocator}}"
             >
    <GroupBox BorderThickness="1" BorderBrush="{DynamicResource TrimColor}" Header=" Radio " Margin="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListView Grid.Row="0" BorderThickness="0" ItemsSource="{Binding Path=StreamList}" SelectedItem="{Binding Path=CurrentStream}"
              Behaviours:ListViewColumns.Stretch="true"
              Behaviours:GridViewSort.AutoSort="True">
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn HeaderTemplate="{StaticResource PlayColumnHeaderTemplate}" Width="27">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource PlayButtonStyle}" Command="{Binding Path=PlayStream}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Stream Name" Width="90" Behaviours:GridViewSort.PropertyName="Name">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Controls:LinkLabel Padding="0" Content="{Binding Path=Name}" IsEnabled="{Binding Path=IsLinked}"  LinkLabelBehavior="HoverUnderline">
                                        <EventCommandTriggers:CommandSource.Trigger>
                                            <EventCommandTriggers:EventCommandTrigger RoutedEvent="Controls:LinkLabel.Click" Command="{Binding Path=LinkClicked}"/>
                                        </EventCommandTriggers:CommandSource.Trigger>
                                    </Controls:LinkLabel>
                                </DataTemplate>

                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Path=Description}" Behaviours:GridViewSort.PropertyName="Description"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Margin="3" Padding="5,0" Command="{Binding Path=ExportStreams}">Export</Button>
                    <Button Margin="3" Padding="5,0" Command="{Binding Path=AddStream}">Add Stream</Button>
                    <Button Margin="3" Padding="5,0" Command="{Binding Path=EditStream}">Edit Stream</Button>
                    <Button Margin="3" Padding="5,0" Command="{Binding Path=DeleteStream}">Remove Stream</Button>
                </StackPanel>

                <Button Grid.Column="2" Margin="3" Padding="5,0" Command="{Binding Path=ImportStreams}">Import</Button>
            </Grid>
        </Grid>
    </GroupBox>
</UserControl>
