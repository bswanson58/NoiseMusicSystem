<UserControl x:Class="Noise.UI.Views.PlaybackContextDialog"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 mc:Ignorable="d"
			 Tag="Playback Context"
			 Height="150"
			 Width="400"
			 >
	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="10"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		
		<StackPanel Grid.Row="0" Orientation="Horizontal">
			<TextBlock VerticalAlignment="Center">Set the following values when playing this</TextBlock>
			<ComboBox Margin="5,0,0,0" ItemsSource="{Binding ContextTypes}" SelectedItem="{Binding CurrentContext}" DisplayMemberPath="ContextName"/>
		</StackPanel>

		<StackPanel Grid.Row="1" Margin="15,0,0,0" Orientation="Horizontal">
			<TextBlock Text="titled: '"/>
			<TextBlock Text="{Binding ContextDescription}"/>
			<TextBlock Text="'"/>
		</StackPanel>

		<CheckBox Grid.Row="3" Margin="5,3" Content="Pan Position" IsChecked="{Binding PlaybackContext.PanPositionValid}" />
		<CheckBox Grid.Row="4" Margin="5,3" Content="Preamp Volume" IsChecked="{Binding PlaybackContext.PreampVolumeValid}" />
		<CheckBox Grid.Row="5" Margin="5,3" Content="Play Speed" IsChecked="{Binding PlaybackContext.PlaySpeedValid}" />
	</Grid>
</UserControl>
