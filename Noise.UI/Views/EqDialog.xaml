<UserControl x:Class="Noise.UI.Views.EqDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="125" Width="450"
             Tag="Parametric Equalization"
             d:DesignHeight="300" d:DesignWidth="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ComboBox Grid.Row="1" ItemsSource="{Binding Path=EqList}" SelectedItem="{Binding Path=CurrentEq, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <CheckBox Grid.Row="2" Content="Enable Eq" IsChecked="{Binding Path=EqEnabled, Mode=TwoWay}" Margin="0,5,0,0"/>
            <Button Grid.Row="3" Margin="0,7,0,0" Content="Reset Eq" Command="{Binding Path=ResetBands}"/>
            <CheckBox Grid.Row="5" Content="Use Replaygain" IsChecked="{Binding Path=ReplayGainEnabled}"/>
        </Grid>
        <Grid Grid.Column="1" Margin="15,5,5,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Slider Grid.Row="0" Maximum="2" Minimum="0" Orientation="Vertical" Margin="5,0"  HorizontalAlignment="Center"
                    Value="{Binding Path=PreampVolume, Mode=TwoWay}"/>
            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Text="Preamp"/>
        </Grid>
        <ItemsControl Grid.Column="2" HorizontalAlignment="Right" Margin="0,5,5,5" ItemsSource="{Binding Path=Bands, Mode=OneWay}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Slider Grid.Row="0" Maximum="15" Minimum="-15" Orientation="Vertical" Margin="0,0,5,0"
                                Value="{Binding Path=Gain, Mode=TwoWay}"  IsEnabled="{Binding Path=IsEditable}"/>
                        <TextBlock Grid.Row="1" HorizontalAlignment="Center" 
                                   Text="{Binding Path=CenterFrequency, Converter={StaticResource NumericTextMultitudeConverter}}"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
