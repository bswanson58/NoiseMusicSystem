<UserControl x:Class="Noise.UI.Views.SelectCategoryDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="70" Width="350"
             Tag="Select Category"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="9"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="1" HorizontalAlignment="Right" Content="Categories:" Margin="5,0,0,0"/>
        <ComboBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding Path=CategoryList}" SelectedItem="{Binding Path=SelectedItem}" DisplayMemberPath="Name" />
    </Grid>
</UserControl>
