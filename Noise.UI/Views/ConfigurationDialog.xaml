<Window x:Class="Noise.UI.Views.ConfigurationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:Behaviours="clr-namespace:Noise.UI.Behaviours"
        mc:Ignorable="d" 
        d:DesignHeight="300" d:DesignWidth="300"
        ShowInTaskbar="False"
        Height="150" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Title="Noise Configuration"
        WindowStyle="ToolWindow"
        DataContext="{Binding DialogModelBase, Source={StaticResource ViewModelLocator}}" Topmost="True"
        Behaviours:DialogCloser.DialogResult="{Binding DialogResult}"
        >
    
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Content="Noise Configuration Settings:" Margin="5,0,0,0"/>
        <CheckBox Grid.Column="0" Grid.Row="1" Content="Update library on application startup." IsChecked="{Binding Path=EditObject.EnableLibraryExplorer}" Grid.ColumnSpan="2" Margin="11,3,0,3"/>
        <CheckBox Grid.Column="0" Grid.Row="2" Content="Enable background metadata updating." IsChecked="{Binding Path=EditObject.EnableBackgroundContentExplorer}" Grid.ColumnSpan="2" Margin="11,3,0,3"/>

        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Command="{Binding Path=Ok}" Width="70" IsDefault="True" Height="22" VerticalAlignment="Bottom" Margin="5,0"/>
            <Button Content="Cancel" Command="{Binding Path=Cancel}" Width="70" Margin="5,0" IsCancel="True" Height="22" VerticalAlignment="Bottom"/>
        </StackPanel>
    </Grid>
</Window>
