<UserControl x:Class="Noise.UI.Views.ConfigurationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="125" Width="350"
        Tag="Noise Options"
        >
    
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Content="Noise Option Settings:" Margin="5,0,0,0"/>
        <CheckBox Grid.Column="0" Grid.Row="1" Content="Update library on application startup." IsChecked="{Binding Path=EnableLibraryExplorer}" Grid.ColumnSpan="2" Margin="11,3,0,3"/>
        <CheckBox Grid.Column="0" Grid.Row="2" Content="Update library on folder changes." IsChecked="{Binding Path=EnableLibraryChangeUpdates}" Grid.ColumnSpan="2" Margin="11,3,0,3"/>
        <CheckBox Grid.Column="0" Grid.Row="3" Content="Enable background metadata updating." IsChecked="{Binding Path=EnableBackgroundContentExplorer}" Grid.ColumnSpan="2" Margin="11,3,0,3"/>
        <CheckBox Grid.Column="0" Grid.Row="4" Content="Minimize To Tray." IsChecked="{Binding Path=MinimizeToTray}" Grid.ColumnSpan="2" Margin="11,3,0,3"/>
    </Grid>
</UserControl>
