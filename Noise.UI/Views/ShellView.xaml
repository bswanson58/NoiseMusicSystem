<UserControl x:Class="Noise.UI.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Infrastructure="clr-namespace:Noise.Infrastructure;assembly=Noise.Infrastructure"
             xmlns:cal="clr-namespace:Microsoft.Practices.Composite.Presentation.Regions;assembly=Microsoft.Practices.Composite.Presentation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
              DataContext="{Binding ShellViewModel, Source={StaticResource ViewModelLocator}}"
             >
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <Grid x:Name="Explorer" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <ContentControl Grid.Column="0" Margin="0,0,7,0" cal:RegionManager.RegionName="{x:Static Infrastructure:RegionNames.LibraryExplorerRegion}"/>
            <GridSplitter Width="7" Height="Auto" Background="Transparent" ResizeDirection="Columns"/>
            <ContentControl Grid.Column="1" cal:RegionManager.RegionName="{x:Static Infrastructure:RegionNames.TrackListRegion}"/>
        </Grid>

        <Grid x:Name="Player" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <ContentControl Grid.Column="0" Margin="0,0,0,7" cal:RegionManager.RegionName="{x:Static Infrastructure:RegionNames.PlayQueueRegion}"/>
            <GridSplitter Width="7" Height="Auto" Background="Transparent" ResizeDirection="Columns"/>
            <ContentControl Grid.Column="1" cal:RegionManager.RegionName="{x:Static Infrastructure:RegionNames.PlaybackRegion}"/>
        </Grid>
    </Grid>
</UserControl>
