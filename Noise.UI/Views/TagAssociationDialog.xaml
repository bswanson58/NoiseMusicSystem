<UserControl x:Class="Noise.UI.Views.TagAssociationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Dto="clr-namespace:Noise.UI.Dto"
             Tag="Tag Associations"
             Height="250" Width="400">

    <UserControl.Resources>
        <DataTemplate x:Key="TagItemTemplate" DataType="Dto:UiTag">
            <StackPanel Orientation="Horizontal">
                <CheckBox Margin="5,0" IsChecked="{Binding IsChecked}"/>
                <TextBlock VerticalAlignment="Center" Text="{Binding Name}"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select the tags to be associated with the track:"/>
        <TextBlock Grid.Row="1" Margin="3,0,0,0" Text="{Binding TrackName}"/>
        <ListBox Grid.Row="2" ItemsSource="{Binding TagList}" ItemTemplate="{StaticResource TagItemTemplate}"/>
    </Grid>
</UserControl>
