<UserControl x:Class="Noise.UI.Views.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="700">
    
    <UserControl.DataContext>
        <Binding Path="ToolbarViewModel" Source="{StaticResource ViewModelLocator}"/>
    </UserControl.DataContext>

    <ToolBarTray DockPanel.Dock="Top">
        <ToolBar Band="1" BandIndex="1">
            <Button Margin="3" Command="{Binding Path=DatabaseConfiguration}">Configuration</Button>
            <Button Margin="3" Command="{Binding Path=LibraryConfiguration}">Library</Button>
            <Button Margin="3" Command="{Binding Path=NoiseOptions}">Options</Button>
            <Button Margin="3" Command="{Binding Path=SmallPlayerView}">Player</Button>
            <Button Margin="3" Command="{Binding Path=LibraryLayout}">Library Layout</Button>
            <Button Margin="3" Command="{Binding Path=StreamLayout}">Stream Layout</Button>
            <Button Margin="3" Command="{Binding Path=Search}">Search</Button>
            <Button Margin="3" Command="{Binding Path=DisplayLog}">Log</Button>
        </ToolBar>
    </ToolBarTray>
</UserControl>
