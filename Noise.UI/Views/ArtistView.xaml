<UserControl x:Class="Noise.UI.Views.ArtistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Noise.UI.ValueConverters"
             xmlns:adapters="clr-namespace:Noise.UI.Adapters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.DataContext>
        <Binding Path="ArtistViewModel" Source="{StaticResource ViewModelLocator}"/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <local:ByteImageConverter x:Key="imageConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <FlowDocumentScrollViewer Grid.Row="0">
            <FlowDocument ColumnWidth="100" IsHyphenationEnabled="True" IsOptimalParagraphEnabled="True">
                <Paragraph>
                        <InlineUIContainer>
                            <Image Width="100" Source="{Binding Path=ArtistImage, Converter={StaticResource imageConverter}}"/>
                        </InlineUIContainer>
                </Paragraph>
                <Paragraph>
                    <adapters:BindableRun BoundText="{Binding Path=ArtistBio}" />
                </Paragraph>
            </FlowDocument>
        </FlowDocumentScrollViewer>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" ItemsSource="{Binding Path=SimilarArtist}"/>
            <ListBox Grid.Column="1" ItemsSource="{Binding Path=TopAlbums}"/>
        </Grid>
    </Grid>
</UserControl>
