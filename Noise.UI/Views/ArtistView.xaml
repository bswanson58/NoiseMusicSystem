<UserControl x:Class="Noise.UI.Views.ArtistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Behaviours="clr-namespace:Noise.UI.Behaviours"
             xmlns:Controls="clr-namespace:Noise.UI.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:EventCommandTriggers="clr-namespace:Noise.UI.Behaviours.EventCommandTriggers"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:Noise.UI.ValueConverters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Height="200"
             Background="White"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <!--
        d:DataContext="{d:DesignInstance BlendableViewModels:BlendableArtistViewModel,
        IsDesignTimeCreatable=True}"
    -->
    <UserControl.Resources>
        <local:ByteImageConverter x:Key="imageConverter" />
    </UserControl.Resources>

    <UserControl.DataContext>
        <Binding Path="ArtistViewModel" Source="{StaticResource ViewModelLocator}" />
    </UserControl.DataContext>

    <i:Interaction.Behaviors>
        <Behaviours:InteractionRequestDialogBehavior DialogClass="ArtistEdit"
                                                     DialogService="{Binding Source={StaticResource DialogService}}"
                                                     InteractionRequest="{Binding Path=ArtistEditRequest}" />
    </i:Interaction.Behaviors>

    <Grid Visibility="{Binding ArtistValid, Converter={StaticResource BooleanVisibilityConverter}}">
        <Border Background="{DynamicResource TreeItemBackground}"
                BorderThickness="0"
                CornerRadius="5">
            <Grid Background="{StaticResource NoiseBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0"
                       MinWidth="50"
                       MinHeight="50"
                       Margin="7,7,3,0"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Source="{Binding ArtistImage,
                                        Converter={StaticResource imageConverter}}"
                       Stretch="Uniform" />
                <Grid Grid.Column="3" Margin="3,0,11,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="ArtistName" Margin="0,0,0,9"
                               HorizontalAlignment="Right"
                               FontSize="20"
                               Text="{Binding Artist.Name}" />

                    <!--  Rating controls  -->
                    <StackPanel Grid.Row="1"
                                Margin="0,1"
                                HorizontalAlignment="Right"
                                Orientation="Horizontal">
                        <CheckBox Margin="0,0,7,0"
                                  VerticalAlignment="Center"
                                  IsChecked="{Binding Artist.UiIsFavorite,
                                                      Mode=TwoWay}"
                                  Style="{StaticResource FavoriteCheckBoxStyle}" />
                        <Controls:RatingControl Rating="{Binding Artist.UiRating, Mode=TwoWay}" Style="{StaticResource NoiseRatingStyle}" />
                    </StackPanel>

                    <TextBlock x:Name="Genre"
                               Grid.Row="2"
                               HorizontalAlignment="Right"
                               Text="{Binding Artist.Genre}" />

                    <!--  Artist website link  -->
                    <Controls:LinkLabel Grid.Row="3"
                                        HorizontalAlignment="Right"
                                        Content="{Binding ArtistWebsite.DisplayText}"
                                        IsEnabled="{Binding ArtistWebsite.IsLinked}"
                                        LinkLabelBehavior="HoverUnderline"
                                        Padding="0">
                        <EventCommandTriggers:CommandSource.Trigger>
                            <EventCommandTriggers:EventCommandTrigger Command="{Binding ArtistWebsite.LinkClicked}" RoutedEvent="Controls:LinkLabel.Click" />
                        </EventCommandTriggers:CommandSource.Trigger>
                    </Controls:LinkLabel>
                    <StackPanel Height="19" Margin="52.014,0,0,5" Orientation="Horizontal" Grid.Row="4" VerticalAlignment="Bottom" d:LayoutOverrides="Width" HorizontalAlignment="Right">
                    	<Button x:Name="ToggleArtistInfo"
                    		Width="19"
                    		HorizontalAlignment="Center"
                    		VerticalAlignment="Top"
                    		Command="{Binding ToggleArtistInfoPanel}"
                    		Content="{StaticResource Path_ToggleView}"
                    		Foreground="{StaticResource ControlTextColor}"
                    		Padding="1"
                    		ToolTip="Open/Close Artist Information View" Margin="0,0,9,0" />

                    	<Button x:Name="EditArtist"
                    		Width="19"
                    		Height="19"
                    		Command="{Binding EditArtist}"
                    		Content="{StaticResource Path_Edit}"
                    		Foreground="{StaticResource ControlTextColor}"
                    		Padding="2"
                    		ToolTip="Edit Artist Properties" />

                    </StackPanel>

                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
