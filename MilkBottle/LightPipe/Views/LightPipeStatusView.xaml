<UserControl x:Class="LightPipe.Views.LightPipeStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mvvm="http://prismlibrary.com/"
             xmlns:viewModels="clr-namespace:LightPipe.ViewModels"
             xmlns:dto="clr-namespace:LightPipe.Dto"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:LightPipeStatusViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="300" Width="160">

    <UserControl.Resources>
        <DataTemplate x:Key="ZoneColorTemplate" DataType="dto:ColorBin">
            <Rectangle Margin="1,0" Height="15" Width="15">
                <Rectangle.Fill>
                    <SolidColorBrush Color="{Binding Color}"/>
                </Rectangle.Fill>
            </Rectangle>
        </DataTemplate>

        <DataTemplate x:Key="MeanColorTemplate" DataType="Color">
            <Rectangle Margin="1,0" Height="15" Width="15">
                <Rectangle.Fill>
                    <SolidColorBrush Color="{Binding}"/>
                </Rectangle.Fill>
            </Rectangle>
        </DataTemplate>

        <DataTemplate x:Key="ZoneSummaryTemplate" DataType="viewModels:ZoneInfo">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <TextBlock Margin="3,0" Text="Zone:"/>
                    <TextBlock Text="{Binding ZoneId}"/>
                </StackPanel>
<!--
                <ListBox Grid.Row="1" Margin="0,2,0,0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding Colors}" ItemTemplate="{StaticResource ZoneColorTemplate}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
-->
                <ListBox Grid.Row="2" Margin="0,2,0,0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding MeanColors}" ItemTemplate="{StaticResource MeanColorTemplate}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Zone Summaries"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding ZoneSummaries}" ItemTemplate="{StaticResource ZoneSummaryTemplate}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <TextBlock Margin="3,0" Text="Processing Time:"/>
            <TextBlock Text="{Binding ElapsedTime}"/>
        </StackPanel>
    </Grid>
</UserControl>
