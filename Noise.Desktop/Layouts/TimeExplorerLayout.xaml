<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Regions="clr-namespace:Microsoft.Practices.Prism.Regions;assembly=Microsoft.Practices.Prism">
    
    <UserControl.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF588CE0"/>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="Content" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="9*" MinWidth="200" />
                <ColumnDefinition Width="2*" MinWidth="200" />
            </Grid.ColumnDefinitions>
            
            <Grid Grid.Column="0" Margin="5,5,5,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="4*" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" MinWidth="200" />
                        <ColumnDefinition Width="1*" MinWidth="200" />
                    </Grid.ColumnDefinitions>

                    <ContentControl Grid.Column="0" Margin="1,0,8,0" Regions:RegionManager.RegionName="TimeExplorerAlbums" />
                    
                    <GridSplitter Grid.Column="0" Width="7" Height="Auto" Background="Transparent" ResizeDirection="Columns"
                                  Style="{DynamicResource GridSplitterStyle}" />
                    
                    <ContentControl Grid.Column="1" Margin="1,0,3,0" Regions:RegionManager.RegionName="TimeExplorerTracks" />
                </Grid>
                
                <ContentControl Grid.Row="1" Margin="1,2,2,0" Regions:RegionManager.RegionName="TimeExplorer" />
            </Grid>
            
            <GridSplitter Grid.Column="0" Width="7" Height="Auto" Background="Transparent" ResizeDirection="Columns"
                          Style="{DynamicResource GridSplitterStyle}" />

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ContentControl Grid.Row="0" Regions:RegionManager.RegionName="Toolbar" />
                <ContentControl Grid.Row="1" Regions:RegionManager.RegionName="PlayQueue" />
            </Grid>
        </Grid>

        <ContentControl Grid.Row="1" Margin="3,0,0,3" Regions:RegionManager.RegionName="Player" />

        <StatusBar Grid.Row="2" Background="{StaticResource AccentBrush}">
            <StatusBarItem>
                <TextBlock Margin="5,0" FontSize="12" Foreground="LightGray" Text="Noise Desktop v0.9"/>
            </StatusBarItem>
        </StatusBar>
        
        <ContentControl Width="595"
                        Height="160"
                        Margin="50,0,0,60"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Regions:RegionManager.RegionName="SimilarSongs" />

        <ContentControl Width="470"
                        Height="600"
                        Margin="120,0,0,60"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Regions:RegionManager.RegionName="SongLyrics" />
    </Grid>
</UserControl>
