<UserControl x:Class="ArchiveLoader.Views.ProcessRecorderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Dto="clr-namespace:ArchiveLoader.Dto"
             xmlns:Mvvm="http://prismlibrary.com/" 
             Mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <DataTemplate x:Key="CompletedItemTemplate" DataType="Dto:CompletedProcessItem">
            <Grid Margin="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Style="{StaticResource ListViewTextStyle}" Text="{Binding FileName}"/>
                <TextBlock Grid.Column="2" Style="{StaticResource ListViewTextStyle}" Text="{Binding ProcessNames}"/>
            </Grid>
        </DataTemplate>

    </UserControl.Resources>

    <Grid Margin="15,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Margin="0,0,5,0" VerticalAlignment="Center" Style="{StaticResource ControlTitleStyle}" Text="Volume:"/>
            <ComboBox Width="150" ItemsSource="{Binding VolumeList}" SelectedItem="{Binding CurrentVolume}"/>
        </StackPanel>

        <ListBox Grid.Row="1" ItemsSource="{Binding ProcessList}" ItemTemplate="{StaticResource CompletedItemTemplate}"/>
    </Grid>
</UserControl>
